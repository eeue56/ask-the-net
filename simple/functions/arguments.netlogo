; this is a function with two arguments
;
to $ [a b]
  show a
  show b
end

; in netlogo, arguments are passed in like so 
$ "dog" 5


; user-defined functions can not take a variable number of arguments
; instead, use a list

to $ [stuff]
  show first stuff
  show last stuff
end

$ ["hello" 3]